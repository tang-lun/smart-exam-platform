# 修改记录 — 2026年5月14日

---

## 一、后端 Bug 修复

### 1. 知识点筛选在分页后执行导致结果错误

- **文件**: `backend/app/api/questions.py` 第 202-204 行
- **问题**: `knowledge_point` 筛选在 SQL 分页（LIMIT/OFFSET）**之后**在 Python 内存中执行，导致：每页返回数量不足（因为先取了 20 条再筛），`total` 计数被错误重置为当前页筛选后的数量
- **修改**: 将知识点筛选移到 SQL 层，用 `LIKE '%"知识点"%'` 匹配 JSON 数组列
- **效果**: 知识点筛选正确地在数据库层面过滤，分页总数准确，每页返回条数符合预期

### 2. 提交试卷评分忽略每题分值

- **文件**: `backend/app/api/exams.py` 第 467-485 行
- **问题**: `submit_exam` 用 `total_score // count` 整数除法均分每题分数，完全忽略 `question_scores` 中存储的每题实际分值。后端存的分值与前端显示的分数不一致
- **修改**:
  - 改用 `question_scores.get(str(i), 0)` 逐题累加正确题目的分值
  - 添加旧试卷向后兼容：若无 `question_scores` 则均分兜底
- **效果**: 评分使用组卷时分配的每题实际分值，后端与前端显示一致

### 3. JSON 转义修复函数破坏 Unicode 转义

- **文件**: `backend/app/services/ai_service.py` `_fix_json_escapes` 函数
- **问题**: 函数把所有 `\字母` 都双写反斜杠（如 `\s` → `\\s`），但 `\uXXXX` 是合法 JSON Unicode 转义，被错误转为 `\\uXXXX` 后丢失原意
- **修改**: 添加特殊判断——当 `\u` 后跟 4 位十六进制数时跳过不处理
- **效果**: Unicode 转义序列保持正确，LaTeX 命令（`\sqrt`、`\frac` 等）的反斜杠仍然被正确修复

---

## 二、前端 Bug 修复

### 4. 答题评分前端本地计算与后端不一致

- **文件**: `frontend/src/views/ExamTake.vue` `doSubmit` 函数
- **问题**: 前端本地用 `question_scores` 计算分数并显示，后端用均分计分（Bug #2），结果不一致。后端返回的权威评分被忽略
- **修改**: 改为先提交后端 → 用后端返回的 `data.score` / `data.correct_count` / `data.answers` 展示结果
- **效果**: 前端显示的分数始终与后端存储的一致，唯一权威来源

### 5. 每题分值计算 0 分时边界值错误

- **文件**: `frontend/src/views/ExamTake.vue` `scorePerQuestion` 计算属性
- **问题**: `Number(scores[i] || 0) || fallback` — 当分值为 `0` 时，`0 || 0` → `0`，然后 `0 || fallback` 错误使用兜底值
- **修改**: 改为 `val != null ? Number(val) : fallback`，严格区分 `0` 和 `null/undefined`
- **效果**: 分值为 0 时不再错误跳到兜底均分值

### 6. 填空题题干出现裸露 `\text` 符号

- **文件**: 10 个文件（见下方详表）
- **问题**: AI 生成填空题时用 `$$\text{______}$$` 表示填空线。`\text{______}` 中多个下划线 `_` 在 LaTeX 里是下标符号，KaTeX 渲染失败，`\text` 原文裸露
- **修改**（5 层防护）:

| 层 | 文件 | 措施 |
|------|------|------|
| 提示词 | `backend/app/services/ai_service.py` | 新增规则 11：严禁 `\text{______}` / `\underline{}`，改用普通文本 `______` 放在公式外 |
| 后端保存 | `backend/app/api/questions.py` | 新增 `_clean_stem()` 正则替换函数，在 generate / manual / import 保存前自动清理 |
| 前端渲染 | `QuestionCard.vue` `ExamTake.vue` `QuestionBank.vue` `CreateExam.vue` `Favorites.vue` `QuestionForm.vue` | `renderMath` 入口处加正则清理 `\text{___}` → `______` |
| 历史数据 | `app.db` | 清理了 5 道旧题目中的不规范标记 |

- **效果**: 新生成的题目不再出现 `\text{______}`，旧数据已被清理，前端有兜底保护

### 7. 证明题只写"成立"就能得分

- **文件**: `backend/app/api/exams.py` `submit_exam` 评分逻辑
- **问题**: AI 在证明题 `answer` 字段只存结论词（"成立""全等""相等"等 2 字词），评分做精确字符串匹配且无最低字数要求。学生写"成立"即满分
- **修改**:
  - 短结论（≤5字）：学生答案需**包含**结论词 **且 ≥15 字**
  - 长结论（>5字）：学生答案需**精确匹配** **且 ≥10 字**
- **效果**: 写"成立"（2 字）无法通过，必须写出完整证明过程（≥15字）并包含正确结论

### 8. `<br>` 标签在题干中裸露显示

- **文件**: 7 个前端文件 + 数据库
- **问题**: AI 生成题目使用 `\n` 分隔多问題小题，前端 `renderMath` 将其替换为 `<br>` 企图换行。但在 Element Plus 表格 tooltip 等非 `v-html` 场景下，`<br>` 作为纯文本裸露显示
- **修改**:
  - 所有 7 个 Vue 文件的 `renderMath`：`\n` → 空格（而非 `<br>`）
  - 数据库：清理 3 道已有题目的 `\n`
- **效果**: 题目题干自然连续显示，不再依赖 HTML 渲染，tooltip 等场景也不会出现 `<br>` 字符

---

## 三、新增功能

### 9. 删除题目后 ID 自动重排

- **文件**: `backend/app/api/questions.py` `delete_question` 端点
- **功能**: 删除题目时，所有 ID 大于被删题目的题目自动 ID 减 1，填补空位。同时同步更新：
  - 所有试卷的 `question_ids` 引用 → 被删 ID 移除，其余 ID 按映射更新
  - `question_scores` → 被删位置之后的分值前移
  - 防止同一题目 ID 在试卷中重复出现
- **测试**: 删除 ID=57 → ID 60→59, 61→60, 62→61… 全部自动下移，试卷引用同步更新
- **效果**: 删除题目不会在 ID 序列中留下空位，ID 保持连续

### 10. 批量生成 76 道题目

- **文件**: `backend/generate_60.py`（脚本，已删除）
- **结果**: 调用 DeepSeek API 20 次，生成 76 道题目，覆盖全部 4 种题型 × 3 种难度 × 初一/初二/初三知识点

| 题型 | 基础 | 中等 | 较难 | 小计 |
|------|:---:|:---:|:---:|:---:|
| 选择题 | 10 | 10 | 8 | 28 |
| 填空题 | 10 | 8 | 3 | 21 |
| 计算题 | 5 | 8 | 5 | 18 |
| 证明题 | 2 | 3 | 4 | 9 |
| **合计** | 27 | 29 | 20 | **76** |

---

## 四、环境适配

### Python 3.14 兼容性

- **问题**: Python 3.14 与 SQLAlchemy 2.0.36 不兼容，`Mapped[list | None]` 类型注解导致 `TypeError`
- **修复**: 升级 SQLAlchemy 至 2.0.49

### 端口残留进程

- **问题**: 多次重启后端导致端口 8000 被旧进程占用，新进程启动失败或路由异常
- **处理**: 手动清理残留进程后重启
